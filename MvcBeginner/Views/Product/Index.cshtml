@using MvcBeginner.Models
@model List<Product>

	<h1>Danh Sách Sản Phẩm</h1>
	@foreach(var product in Model)
{
	<p>
		Id: @product.Id |
	Ten: @product.Name |
	Gia: @product.Price |
	kho du tru: @product.Stock|
	hang muc: @product.Category.Name|
	nguoi cung cap: @product.Supplier.Name
	</p>
}